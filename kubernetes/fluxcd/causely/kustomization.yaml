apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- release.yaml
- repository.yaml

postBuild:
  substitute:
    CAUSELY_VERSION: "v1.0.130-0-g41ae72810eb3d3cf"
  substituteFrom:
    - kind: Secret
      name: causely-secrets