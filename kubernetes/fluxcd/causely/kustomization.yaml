apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- release.yaml
- repository.yaml

postBuild:
  substitute:
    CAUSELY_VERSION: "v1.0.127-0-g851f1d00dcde867e"
  substituteFrom:
    - kind: Secret
      name: causely-secrets