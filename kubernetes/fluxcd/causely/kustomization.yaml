apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- release.yaml
- repository.yaml

postBuild:
  substitute:
    CAUSELY_VERSION: "v1.0.129-0-g7ef05a41ae8c7e2d"
  substituteFrom:
    - kind: Secret
      name: causely-secrets