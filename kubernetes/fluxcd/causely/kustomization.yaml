apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- release.yaml
- repository.yaml

postBuild:
  substitute:
    CAUSELY_VERSION: "v1.0.114-0-g2ba6e0ff6fe89cec"
  substituteFrom:
    - kind: Secret
      name: causely-secrets